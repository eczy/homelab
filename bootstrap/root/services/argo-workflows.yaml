apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argo-workflows
  namespace: argocd
spec:
  project: default
  source:
    repoURL: REPLACE-BY-PATCH
    targetRevision: master
    path: bootstrap/argo-workflows
  destination:
    server: REPLACE-BY-PATCH
    namespace: argo-workflows